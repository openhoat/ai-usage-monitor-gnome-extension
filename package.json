{
  "name": "ai-usage-monitor",
  "version": "1.0.0",
  "private": true,
  "description": "Monitor Claude.ai subscription usage in GNOME Shell",
  "scripts": {
    "build": "tsc",
    "dev": "npm run build && bash install.sh && dbus-run-session -- gnome-shell --wayland",
    "start": "node dist/fetch-usage.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "volta": {
    "node": "24.13.0"
  }
}
